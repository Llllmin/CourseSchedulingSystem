"""Core package for the Ulink IB course scheduling system.

The package is split into small modules:
- database.py creates and opens the SQLite database.
- seed_data.py inserts anonymous demo data.
- scheduler.py generates and validates timetables.
- gui.py displays the Tkinter user interface.
"""
